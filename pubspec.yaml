name: local_first_sync_kit
description: "A local-first sync queue for Flutter apps with retries, conflict hooks, and UI-friendly sync status."
version: 0.0.1
repository: https://github.com/AmirhosseinDarvishi/local_first_sync_kit
issue_tracker: https://github.com/AmirhosseinDarvishi/local_first_sync_kit/issues
topics:
  - flutter
  - offline-first
  - sync
  - queue

environment:
  sdk: ^3.12.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
